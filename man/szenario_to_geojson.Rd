% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geojson_export.R
\name{szenario_to_geojson}
\alias{szenario_to_geojson}
\title{Convert Szenario_Berechnung Output to GeoJSON}
\usage{
szenario_to_geojson(
  szenario_tibble,
  gemeinde_sf,
  scenario_ids = character(0),
  grenzwert_abdeckung = 99,
  as_json = TRUE
)
}
\arguments{
\item{szenario_tibble}{A tibble as returned by \code{\link[=Szenario_Berechnung]{Szenario_Berechnung()}}
with \code{Verwaltungsebene = "Gemeinde"}.}

\item{gemeinde_sf}{An \code{sf} object of Gemeinde polygon geometries with at
least a \code{Gemeindeschluessel} column. Optionally a \code{Gemeindename} column
for fallback names.}

\item{scenario_ids}{Character vector of hospital \code{Standortnummer} IDs used
to create the scenario (default: empty).}

\item{grenzwert_abdeckung}{Numeric coverage threshold in percent (default 99).}

\item{as_json}{Logical; if \code{TRUE} (default), return a JSON string. If
\code{FALSE}, return the R list structure.}
}
\value{
Either a JSON string or a named list representing a GeoJSON
\code{FeatureCollectionCalculate}.
}
\description{
Transforms the tibble returned by \code{\link[=Szenario_Berechnung]{Szenario_Berechnung()}} (at Gemeinde
level) into a GeoJSON FeatureCollection matching the structure produced by
the Python \verb{/calculate} endpoint.
}
\details{
The pipeline:
\enumerate{
\item Filter by coverage threshold (\code{\link[=filter_by_coverage]{filter_by_coverage()}})
\item Build scenario properties (\code{\link[=build_scenario_properties]{build_scenario_properties()}})
\item Add empty scenario-2 columns (\code{\link[=add_empty_scenario2_columns]{add_empty_scenario2_columns()}})
\item Join onto polygon geometries (\code{\link[=join_properties_to_polygons]{join_properties_to_polygons()}})
\item Convert to GeoJSON feature list (\code{\link[=sf_to_feature_list]{sf_to_feature_list()}})
\item Assemble FeatureCollection (\code{\link[=assemble_feature_collection]{assemble_feature_collection()}})
}
}
\examples{
# geojson <- szenario_to_geojson(
#   szenario_tibble = Szenario_Berechnung(con, standort_nummern),
#   gemeinde_sf = my_gemeinde_polygons,
#   scenario_ids = standort_nummern
# )

}
