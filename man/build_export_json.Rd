% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geojson_export.R
\name{build_export_json}
\alias{build_export_json}
\title{Build Full Export JSON (ExportData Format)}
\usage{
build_export_json(
  clinics,
  results,
  results_mode = "fahrtzeit_scenario_1",
  result_geo_filters = list(bundesland = list(), kreis = list(), gemeinde = list()),
  as_json = TRUE
)
}
\arguments{
\item{clinics}{R list from \code{\link[=kliniken_to_geojson]{kliniken_to_geojson()}} with \code{as_json = FALSE}.}

\item{results}{R list from \code{\link[=szenario_to_geojson]{szenario_to_geojson()}} with \code{as_json = FALSE}.
The scenario IDs for \code{checkedClinicsScenario1} and \code{checkedClinicsScenario2}
are read from \code{results$scenario1$list} and \code{results$scenario2$list}.}

\item{results_mode}{Character string controlling which result field the
frontend will display on load. One of \code{"fahrtzeit_scenario_1"} (default),
\code{"fahrtzeit_scenario_2"}, or \code{"fahrtzeit_difference"}.}

\item{result_geo_filters}{Named list with elements \code{bundesland}, \code{kreis},
\code{gemeinde} (each a character vector). Defaults to all-empty (no filter).}

\item{as_json}{Logical; if \code{TRUE} (default), return a JSON string. If
\code{FALSE}, return the R list structure.}
}
\value{
Either a JSON string or a named list matching the frontend
\code{ExportData} schema.
}
\description{
Combines the outputs of \code{\link[=kliniken_to_geojson]{kliniken_to_geojson()}} and \code{\link[=szenario_to_geojson]{szenario_to_geojson()}}
into the \code{ExportData} format used by the frontend's "JSON-Daten exportieren"
function (\code{exportDataAsJSON}). The resulting JSON can be re-imported into
the frontend application.
}
\examples{
# results_list <- szenario_to_geojson(..., as_json = FALSE)
# clinics_list <- kliniken_to_geojson(..., as_json = FALSE)
# json <- build_export_json(clinics = clinics_list, results = results_list)

}
