% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_strategies.R
\name{postgres_source}
\alias{postgres_source}
\title{Create PostgreSQL source configuration}
\usage{
postgres_source(
  work_mem = "512MB",
  maintenance_work_mem = "512MB",
  effective_cache_size = "4GB",
  random_page_cost = 1.1,
  effective_io_concurrency = 200,
  max_parallel_workers_per_gather = 4
)
}
\arguments{
\item{work_mem}{Memory for sort/hash operations (default: "512MB")}

\item{maintenance_work_mem}{Memory for maintenance ops (default: "512MB")}

\item{effective_cache_size}{Planner's cache size assumption (default: "4GB")}

\item{random_page_cost}{Cost estimate for random page fetch (default: 1.1)}

\item{effective_io_concurrency}{Concurrent I/O operations (default: 200)}

\item{max_parallel_workers_per_gather}{Parallel workers per query (default: 4)}
}
\value{
A pg_source object
}
\description{
Creates a configuration object for using PostgreSQL as the data source.
Settings are applied via SET commands when connecting. If a setting is NULL,
it will not be applied (database default will be used).
}
\examples{
\dontrun{
# Use defaults
src <- postgres_source()

# Override specific settings
src <- postgres_source(work_mem = "1GB", max_parallel_workers_per_gather = 8)

# Use database defaults for all settings (no optimizations)
src <- postgres_source(
  work_mem = NULL,
  maintenance_work_mem = NULL,
  effective_cache_size = NULL,
  random_page_cost = NULL,
  effective_io_concurrency = NULL,
  max_parallel_workers_per_gather = NULL
)
}
}
